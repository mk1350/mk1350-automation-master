# 自动化办公大师

## 📌 技能概述
一站式办公自动化套件，集成文件处理、数据提取、财税智能同步等核心功能。一次安装，拥有全套办公效率工具。

**版本**：1.0.0  
**开发者**：李涛 
**分类**：办公自动化 / 效率工具 / 财税工具 / 会计工具 / 人力资源工具 
**标签**：财税同步、智能对账、发票数据（增值税、铁路电子、机票）提取、模板生成、数据合并、文件转换、批量重命名


## 🎯 核心功能

### 📂 文件处理类
| 功能 | 说明 | 调用方式 |
|------|------|---------|
| 文件转换 | PDF/Word/Excel/图片互转 | `action=convert` |
| 批量重命名 | 按规则批量重命名文件 | `action=rename` |
| 数据堆叠 | 多个Excel文件/工作表上下合并（效果：同样字段、表结构下的复制粘贴） | `action=concat` |
| 数据拼接 | 多个Excel文件按主键合并（效果：类似VLOOKUP，但比Excel快10倍） | `action=merge` |
| 模板生成 | 根据数据批量生成文档 | `action=generate` |
    📄 **模板生成示例**

    **模板语法**
    - 单个字段：`{{字段名}}`
    - 循环：`{% for item in records %}` ... `{% endfor %}`
    - 支持计算：`{{item.数量 * item.单价}}`

    **Excel模板示例**
    ```excel
    {% for item in records %}
    {{item.商品名称}}  {{item.数量}}  {{item.单价}}  {{item.数量 * item.单价}}
    {% endfor %}

### 💰 财税处理类
| 功能 | 说明 | 调用方式 |
|------|------|---------|
| 发票信息提取 | 从PDF/DOCX提取发票信息 | `action=invoice_extract` |
| 财税智能对账 | 税局数据vs财务数据对账 | `action=tax_reconcile` |

## 💰 定价与付费模式

### 1. 免费版（基础功能限量）
| 功能 | 免费额度 | 说明 |
|------|----------|------|
| 文件转换 | 20个文件 | PDF/Word/Excel互转 |
| 批量重命名 | 20个文件 | 按规则批量重命名 |
| 数据合并 | 3次 | 合并Excel|
| 模板生成 | 20个文件 | 根据Excel批量生成文档 |

### 2. 按次付费（核心功能）
| 功能 | 单价 | 适用场景 |
|------|------|----------|
| 发票信息提取 | **1元/张** | 单张/批量提取 |
| 财税智能对账 | **49元/次** | 月度/季度对账 |

### 3. 专业版订阅（高频用户）
| 版本 | 价格 | 权益 |
|------|------|------|
| 月付 | 298元/月 | 全部功能不限量 |
| 年付 | 2980元/年 | 送2个月（相当于248元/月） |

## 📄 模板生成示例

### Excel模板语法
支持 `docxtpl` 风格的占位符和循环：

```excel
{% for item in records %}
{{item.product_name}}  {{item.quantity}}  {{item.price}}  {{item.quantity * item.price}}
{% endfor %}

## 🏆 效果保证

### 发票提取
| 准确率 | 处理方式 |
|--------|---------|
| **≥98%** | 正常计费 |
| **<98%** | **本次免费** |

### 财税对账
| 准确率 | 处理方式 |
|--------|---------|
| ≥80% | 正常计费 |
| **<80%** | **本次免费** |

 **技术说明**：
本技能提供两种数据维度，由用户自行选择：

**1. 合计数据模式（version=basic）**
- 提取内容：发票级数据
- 核心字段：发票号码、开票日期、购买方名称、购买方税号、合计金额、合计税额、税率
- 准确率计算：成功提取核心字段的发票张数 ÷ 总处理发票张数

**2. 明细数据模式（version=complete）**
- 提取内容：商品明细行数据
- 核心字段：商品名称、商品金额、商品税额、商品税率（每行）
- 准确率计算：成功提取的商品明细行数 ÷ 总商品明细行数
- 一行商品成功的标准：4个核心字段全部提取成功

**低于98%自动退款，无需用户申请**

## 🔐 权限控制规则

| 功能 | 免费版 | 按次付费 | 专业版 |
|------|--------|----------|--------|
| 文件转换 | 20个文件 | - | 不限 |
| 批量重命名 | 20个文件 | - | 不限 |
| 数据合并 | 3次 | - | 不限 |
| 模板生成 | 20个文件 | - | 不限 |
| 发票提取 | 2次试用 | 1元/张 | 不限 |
| 财税对账 | 2次试用 | 49元/次 | 不限 |

